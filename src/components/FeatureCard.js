import React from "react";
import styled from "styled-components";

export default function FeatureCard({className, title, icon}) {
    return (
        <div>
            {/* TODO - Square background with an icon and text underneath */}
        </div>
    )
}